# === Directus Configuration ===

# URL de l'instance Directus
NEXT_PUBLIC_DIRECTUS_URL=https://directus.opaleplus.cloud

# Token statique d'authentification Directus
# Obtenu depuis les paramètres utilisateur dans Directus
DIRECTUS_TOKEN=your_static_token_here

# === Configuration avancée (optionnel) ===

# Durée du cache en millisecondes (par défaut: 300000 = 5 minutes)
# DIRECTUS_CACHE_DURATION=300000

# Timeout des requêtes API en millisecondes (par défaut: 30000 = 30 secondes)
# DIRECTUS_REQUEST_TIMEOUT=30000

# Mode debug (affiche les requêtes API)
# DIRECTUS_DEBUG=false

# Nombre maximum d'éléments par requête (par défaut: 100)
# DIRECTUS_MAX_LIMIT=100

# === Configuration de développement ===

# Logs détaillés en développement
# DEBUG=directus:*

# === Configuration de production ===

# Durée augmentée du cache en production
# DIRECTUS_CACHE_DURATION=600000  # 10 minutes

# Notes de sécurité:
# - Ne commitez jamais .env.local dans git
# - Gardez DIRECTUS_TOKEN secret (serveur seulement)
# - En production, utilisez les secrets de votre plateforme
# - NEXT_PUBLIC_DIRECTUS_URL peut être public
